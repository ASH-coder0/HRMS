require('dotenv').config();

const {
  PORT,
  NODE_ENV,
  DB_HOST,
  DB_USERNAME,
  DB_PASSWORD,
  DB_NAME,
  DB_PORT,
  JWT_SECRET,
  JWT_EXPIRY,
  REFRESH_SECRET,
  REFRESH_EXPIRY,
  REACT_APP_URL,
  REACT_ADMIN_APP_URL,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_USER,
  SMTP_PASSWORD,
  SMTP_FROM,
} = process.env;

module.exports = {
  PORT: PORT || 5000,
  NODE_ENV: NODE_ENV || 'development',
  DB_HOST,
  DB_USERNAME,
  DB_PASSWORD,
  DB_NAME,
  DB_PORT,
  JWT_SECRET,
  JWT_EXPIRY,
  REFRESH_SECRET,
  REFRESH_EXPIRY,
  REACT_APP_URL,
  REACT_ADMIN_APP_URL,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_USER,
  SMTP_PASSWORD,
  SMTP_FROM,
};
